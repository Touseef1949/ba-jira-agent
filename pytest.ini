[pytest]
markers =
    smoke: Quick smoke tests that verify core functionality
    unit: Unit tests for individual components
    integration: Integration tests that may touch multiple components
    regression: Regression tests for previously fixed bugs
    slow: Slow-running tests that may be skipped in fast CI runs
    app: Tests for the Streamlit application layer
testpaths = tests
python_files = test_*.py
