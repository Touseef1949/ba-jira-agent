[theme]
base = "light"
primaryColor = "#0C66E4"
backgroundColor = "#F7F8FA"
secondaryBackgroundColor = "#FFFFFF"
textColor = "#172B4D"
font = "sans serif"

[server]
# Default port for local dev; HF Spaces overrides via CMD
port = 8503
maxUploadSize = 10

[browser]
gatherUsageStats = false
